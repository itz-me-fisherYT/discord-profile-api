# Discord Profile API

Real-time Discord profile API and bot for displaying **ItzRealFisher’s** status and avatar on a website.

---

## Features

- Fetches Discord username, tag, and avatar in real-time.
- Updates every 10 seconds via a small Node.js bot.
- Serves a `/profile.json` endpoint for websites to fetch profile data.
- Works with GitHub Pages, Railway, Render, or any Node.js server.

---

## Installation

1. Clone the repo:

```bash
git clone https://github.com/yourusername/discord-profile-api.git
cd discord-profile-api
